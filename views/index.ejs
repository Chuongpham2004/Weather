<%- include('partials/header') %>

  <main class="main-content">
    <section class="hero-section">
      <div class="container">
        <div class="hero-content">
          <h1>
            <%= __('home.hero.heading') %>
          </h1>
          <p class="hero-subtitle">
            <%= __('home.hero.subheading') %>
          </p>

          <form action="/weather" method="GET" class="search-form" id="weather-search">
            <div class="search-wrapper">
              <span class="search-icon">🔍</span>
              <input type="text" name="city" placeholder="<%= __('home.search.placeholder') %>" class="search-input"
                value="<%= city || '' %>" required autocomplete="off"
                aria-label="<%= currentLang === 'vi' ? 'Tên thành phố' : 'City name' %>">
              <button type="submit" class="search-btn">
                <span>
                  <%= __('home.search.button') %>
                </span>
                <span class="btn-arrow">→</span>
              </button>
            </div>
          </form>

          <% if (typeof error !=='undefined' && error) { %>
            <div class="error-message" role="alert">
              <span class="error-icon">⚠️</span>
              <p>
                <%= error %>
              </p>
            </div>
            <% } %>
        </div>

        <div class="hero-features">
          <div class="feature-card">
            <span class="feature-icon">⏰</span>
            <h3>
              <%= __('home.features.hourly.title') %>
            </h3>
            <p>
              <%= __('home.features.hourly.description') %>
            </p>
          </div>
          <div class="feature-card">
            <span class="feature-icon">📅</span>
            <h3>
              <%= __('home.features.daily.title') %>
            </h3>
            <p>
              <%= __('home.features.daily.description') %>
            </p>
          </div>
          <div class="feature-card">
            <span class="feature-icon">🌍</span>
            <h3>
              <%= __('home.features.global.title') %>
            </h3>
            <p>
              <%= __('home.features.global.description') %>
            </p>
          </div>
        </div>
      </div>
    </section>

    <section class="popular-cities">
      <div class="container">
        <h2>
          <%= __('home.popularCities.title') %>
        </h2>
        <div class="cities-grid">
          <a href="/weather?city=Hanoi" class="city-card">
            <span class="city-icon">🏛️</span>
            <span class="city-name">
              <%= currentLang==='vi' ? 'Hà Nội' : 'Hanoi' %>
            </span>
          </a>
          <a href="/weather?city=Ho Chi Minh" class="city-card">
            <span class="city-icon">🌆</span>
            <span class="city-name">
              <%= currentLang==='vi' ? 'Hồ Chí Minh' : 'Ho Chi Minh' %>
            </span>
          </a>
          <a href="/weather?city=Da Nang" class="city-card">
            <span class="city-icon">🏖️</span>
            <span class="city-name">
              <%= currentLang==='vi' ? 'Đà Nẵng' : 'Da Nang' %>
            </span>
          </a>
          <a href="/weather?city=Tokyo" class="city-card">
            <span class="city-icon">🗼</span>
            <span class="city-name">Tokyo</span>
          </a>
          <a href="/weather?city=Singapore" class="city-card">
            <span class="city-icon">🏙️</span>
            <span class="city-name">Singapore</span>
          </a>
          <a href="/weather?city=London" class="city-card">
            <span class="city-icon">🎡</span>
            <span class="city-name">London</span>
          </a>
        </div>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>
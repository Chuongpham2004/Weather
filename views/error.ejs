<%- include('partials/header') %>

    <main class="main-content error-page">
        <div class="container">
            <div class="error-content">
                <div class="error-icon">
                    <% if (errorCode===404) { %>
                        🔍
                        <% } else { %>
                            ⚠️
                            <% } %>
                </div>
                <h1>
                    <% if (errorCode===404) { %>
                        <%= __('error.404.heading') %>
                            <% } else { %>
                                <%= __('error.500.heading') %>
                                    <% } %>
                </h1>
                <p class="error-code">
                    <%= __('error.errorCode') %>: <%= errorCode %>
                </p>
                <p class="error-message">
                    <%= errorMessage %>
                </p>

                <div class="error-actions">
                    <a href="/" class="btn-primary">
                        <span>🏠</span>
                        <%= __('error.actions.home') %>
                    </a>
                    <a href="javascript:history.back()" class="btn-secondary">
                        <span>←</span>
                        <%= __('error.actions.back') %>
                    </a>
                </div>

                <div class="error-suggestions">
                    <h3>
                        <%= __('error.suggestions.title') %>
                    </h3>
                    <ul>
                        <li>
                            <%= __('error.suggestions.checkUrl') %>
                        </li>
                        <li>
                            <%= __('error.suggestions.searchOther') %>
                        </li>
                        <li>
                            <%= __('error.suggestions.refresh') %>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>